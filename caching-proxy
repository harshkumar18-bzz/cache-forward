#!/usr/bin/env sh
set -eu
project_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
jar="$project_dir/target/caching-proxy-0.0.1-SNAPSHOT.jar"
if [ ! -f "$jar" ]; then
    echo 'Build the project first: mvn package' >&2
    exit 1
fi
exec java -jar "$jar" "$@"
